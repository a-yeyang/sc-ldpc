# 科研分析报告：用强化学习做空间耦合 LDPC 的端到端构造优化

> 把整个项目的科学问题、方法、全部实验与发现，统一成一条连贯的科学叙事，并给出诚实的贡献界定、
> 局限与后续方向。技术实现与逐项实验记录见 [RL_CONSTRUCTION.md](RL_CONSTRUCTION.md)；投稿去向见
> [PUBLISHING_STRATEGY.md](PUBLISHING_STRATEGY.md)。代码与数据全部开源在本仓库。

---

## 摘要

空间耦合 LDPC（SC-LDPC）由一张分量码基图通过**边扩展（edge spreading）**——把系统位基边分配到
`w+1` 个耦合分量 `B_0..B_w`——构造而成。这是一个**高杠杆却长期被随机化处理**的设计自由度：实测仅这一个
随机选择就能让同一工作点的误帧率（FER）相差约 7 倍。本工作**首次**把该构造过程建成马尔可夫决策过程
（MDP），用策略梯度（REINFORCE）以**真实收发链的端到端 FER** 为奖励来优化它，并与同预算随机搜索、
交叉熵方法（CEM）、人工启发式、仓库默认随机构造系统对比。

两条主要结论：**(1)** 优化边扩展把 FER 压到默认/朴素构造的 1/5–1/11、并消除错误地板，机理是消除提升
图中的 4 环，且该增益在码率 0.5–0.92 全程成立；**(2)** 更重要、也更干净的一条——**RL 相对随机搜索的优势
随"单次评估成本"单调上升**：评估便宜（中短码大预算、或廉价结构代理）时 RL ≈ 随机搜索 ≈ CEM；评估昂贵
（长码蒙特卡洛）时 RL 在 13/15 个组合上胜出，且优势随耦合记忆 `w`（即搜索空间 `(w+1)^E`）增大而放大。
这把一个看似平淡的"RL 不总赢"现象，转化为一条**对真实码设计有指导意义的判据：目标函数越贵，越该用 RL。**

---

## 1. 问题与动机

- **SC-LDPC 与边扩展**：把一串独立 LDPC 分量码沿空间方向耦合成长链，终止后在 BP 译码下可逼近 MAP 门限
  （阈值饱和，KRU 2011/2013）。构造时，分量基图 `B` 的每条系统位边要分到 `B_0..B_w` 之一（变量位置 `t` 经
  `B_a` 连到校验位置 `t+a`），这就是边扩展。不同的边扩展**同时影响 BP 性能与距离/环结构**
  （Mitchell–Lentmaier–Costello 2015 明确指出并以 Table III 量化）。
- **被忽视的自由度**：主流实现（含本仓库原始版）把系统位边**随机**分到各分量。探针实验：固定信道、只换构造，
  14 个随机边扩展的 FER 跨度 0.106→0.725（~7×）。**这个旋钮值得优化，且优化空间巨大**（`(w+1)^E`，`E≈40–172`，
  `w=3` 时 `4^121`，`w=30` 时 `31^121`）。
- **为何用 RL**：边扩展是**天然序贯**的分配过程，质量由提升图的环结构主导、对每条边的分配高度敏感——这正是
  策略梯度可顺结构梯度爬升、而盲搜难以高效覆盖的地方。

---

## 2. 方法

**把构造建成 MDP**：按固定顺序逐条边地"造码"。回合 = 造一整张码并端到端评估；时间步 = 给第 `e` 条边选分量
`a∈{0..w}`；状态 = 该边在**当前部分构造**下的特征（所在基图行/列已分到各分量的负载占比、该分量对此行/列是否
还空——这些运行计数使其成为真正的序贯 MDP，让策略学到"同列边散开"这类结构规则）；奖励 = **回合末**用真实
`编码→BPSK/PAM4+AWGN→滑窗 BP→FER` 链路测得的 `-FER`（真值仅设计期打分，学到的构造本身不含真值、可部署）。

**两种策略**（均用 REINFORCE，批内基线 + 公共随机数降方差，纯 NumPy）：
- **特征策略**：对"每分量"特征做线性 softmax，参数仅 `w+1` 个偏置 + 几个共享权重 = **8 个数、与码规模无关 →
  可零样本迁移**到不同 `Z/L/码率`，且可解释。
- **逐边 logits 策略**：每条边一套独立 logits（最具表达力但不迁移），作为消融。

**基线（同预算、同评估器、同验证）**：随机搜索（关键对照）、CEM、round-robin、默认随机 seed0、未耦合分量码。
**任何差异只来自搜索策略本身。**

**工程**：评估器对所有构造施加**完全相同的信息比特与噪声**（CRN，已验证逐比特确定）；多进程铺满 CPU；
策略梯度经**有限差分校验**；支持断点续跑、原子写。

**新颖性（经文献核对）**：**没有任何已发表工作把 SC-LDPC 的构造/边扩展建成 MDP 并用 RL 优化。** RL-造码只在
分组码上（华为 AI Coding T-COM 2020；WCSP 2018 的 MCTS 逐边生长）；RL 在 SC-LDPC 上只做过**译码**（RELDEC）；
SC-LDPC 构造优化的既有方法全是贪心/穷举/MCMC/图论（Mitchell、Battaglioni、Esfahanizadeh/Dolecek、ESRL）。
方法学模板：Bello 等"神经组合优化"2016。

---

## 3. 实验与发现（统一叙事）

### 3.1 构造决定成败（最硬、与码率无关）
@2.5 dB、1500–2000 帧高精度复测：优化构造（RL/CEM/随机搜索）FER 0.06–0.13，而**仓库默认 seed0=0.70、朴素
均衡 round-robin=0.66**——优化把 FER 降 **~5–11×**。BER 瀑布显示：默认/朴素构造有**错误地板**（~5e-5 起翘），
优化后**无地板**（降到 ~1e-6/0）。**机理 = 4 环**：默认/朴素 n4≈930、围长 4；优化后 n4=0、围长 6。特征策略
学到的权重读出来就是一条可解释规则——**"把同一列的系统位边散到不同分量"**（正是 4 环避免）。

### 3.2 全码率扫描 0.5–0.9：增益随码率递减
低码率用 BG2、高码率用 BG1，逐码率优化。以 FER=0.1 瀑布门限衡量，**优化构造在全码率都优于默认**，门限增益
**0.12–0.92 dB，中码率 R≈0.6 最大（~0.9 dB）、向高码率单调缩小**——与"阈值饱和填补 BP–MAP 门限差、该差在
高码率本就小"的理论自洽。

### 3.3 RL vs 随机搜索：取决于"评估成本"（核心科学结论）
- **中短码、大预算**（Z=16/32，每方法 ~640–1152 次评估）：**RL ≈ CEM ≈ 随机搜索**——best-of-N 随机暴力就能
  达到同样的 n4=0 / 低 FER。
- **长码、蒙特卡洛**（BG1 Z=32，~860–1470 bit；单帧 ~0.9 s 起、贵评估）：**RL 在 13/15 个组合上胜随机搜索，
  且优势随 `w` 增大而放大**（如 R=2/3：w=3→8，随机搜索 0.040→0.083→0.133 越来越差，而 RL 0.017→0.010→0.000
  越来越好；默认构造几乎全 1.0）。
- **大 `w`、廉价代理**（w=3..30，奖励=4 环代理，~0.3 s 且与 w 无关）：**RL 又 ≈ 随机搜索**（都到 n4=0）——
  代理便宜，随机搜索 1400 次暴力就够。
- **统一规律**：三段连起来——**RL 相对随机搜索的价值随单次评估成本单调上升**。这刻画了**何时该用 RL**：目标
  函数贵（长码蒙卡、密度演化/PEXIT 门限）时样本效率是硬通货；目标便宜（短环计数）时直接随机搜索/CEM 即可。

### 3.4 可迁移 + 可解释
8 维特征策略权重**原封搬到新码**（换 `Z`、换码率）贪心造码，低于典型随机与人工启发式——说明学到的是
**结构性原理**而非过拟合；这是随机搜索/CEM 给不了的。

### 3.5 波形级与超参（已有基础设施）
仓库另含 PAM4+RRC 波形信道与码长/链长/深度的参数实验框架，可把构造优化推广到更真实链路（留作后续）。

---

## 4. 核心贡献界定（诚实）

1. **首个把 SC-LDPC 边扩展建成 MDP 并用 RL 端到端优化的工作**（新颖性经文献核对）。
2. **一条干净、可推广的方法论结论**：黑盒组合设计中，**RL 相对随机搜索的优势随单次评估成本单调上升**，并据此
   给出"何时用 RL"的判据。这比"RL 必胜"更有价值——它是对**方法适用边界**的刻画，且诚实。
3. **一个可部署的工程结论**：5G NR SC-LDPC 的边扩展是被忽视的高杠杆旋钮；优化它消除错误地板（5–11× FER），
   且存在一条**可解释、可迁移**的规则（同列边散开 ⇔ 避 4 环）。

---

## 5. 诚实的局限

- **经验性有限长研究，无渐近定理**：没有新的密度演化/门限定理或系综证明；奖励是蒙特卡洛 FER 而非闭式/学到的
  目标。→ 投 T-IT 的硬门槛，也是最该补的。
- **RL 对基线的胜出是"有条件"的**，非无条件碾压——这是本工作诚实的核心，但对 ML 顶会主轨不利（对 ML
  workshop 反而是卖点）。
- **中等规模、纯 CPU**（Z≤128、w≤30、L≤300），非硬件/标准化贡献。
- **大 w + 高码率的蒙卡有标定难点**：耦合码率损失使强码瀑布很陡，自动工作点偶有偏离（少数格 FER=1.0），需更
  鲁棒的工作点选择或换用门限类目标。

---

## 6. 后续方向（按杠杆排序）

1. **把奖励换/加成 DE/PEXIT 门限**（最高杠杆）：补上"只有蒙卡、无理论"的短板，提供**昂贵但有原理**的目标
   （正是"评估贵→RL 赢"论点最锋利处），把工作推向 T-IT/JSAIT 可信度。
2. **一个小组合引理**：把"同列边散开"规则与提升 QC 图的 4 环/girth 计数严格联系，消解"只是仿真"的质疑。
3. **联合优化提升移位**（搜索空间再爆炸，进一步放大 RL 主场）；**元强化学习**跨码率/SNR 产出整族码（对标
   AM-RELDEC，随机搜索结构上做不到）。
4. **与"RL 译码"正交结合**：一边学怎么造、一边学怎么译。

---

## 7. 复现

```bash
python3 tests_construct.py                   # 自检（含策略梯度有限差分校验）
python3 experiments_construct.py all         # R~0.6 深度研究：搜索/曲线/机理/迁移
python3 experiments_construct_rate.py sweep  # 全码率 0.5–0.9 扫描
python3 experiments_construct_big.py slice A # 长码高码率（贵评估，RL 主场）
python3 experiments_construct_wmc.py slice A # 大 w(3–12) 蒙卡 + 链长验证
python3 experiments_construct_wl.py run      # 大 w(3–30) 结构代理扫描
```
数据 `results_*.json` / `results_big_*.csv`，图 `exp_*.svg`，技术细节 `RL_CONSTRUCTION.md`。

---

## 8. 一句话总结

> **边扩展是 SC-LDPC 里被随机化对待的高杠杆构造自由度；优化它能消错误地板、降 FER 5–11×。我们首次用 RL
> 端到端优化它，并诚实地刻画出 RL 的适用边界——目标函数越贵、空间越大，RL 相对盲搜的样本效率优势越值钱。**
