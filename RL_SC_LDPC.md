# 用强化学习优化空间耦合 LDPC 滑窗译码

> 研究记录 / 方法说明。配套实现:[rl_decoder.py](rl_decoder.py)（MDP 环境 + 表格 Q-learning，纯 NumPy）、
> [experiments_rl.py](experiments_rl.py)（与基线对比、出图）。
> 本工作建立在本仓库已有的 5G NR → SC-LDPC 收发链之上（见 [README.md](README.md)）。

---

## 0. 一句话

> **经典 SC-LDPC 滑窗译码器用"整窗综合征"作为停止判据,但它每个位置只输出"目标块";
> 我们用强化学习把滑窗译码建成一个马尔可夫决策过程(MDP),让智能体从奖励中自动学到
> "目标块可靠即提交"的正确判据,从而在同等误码率下大幅降低译码迭代复杂度。**

这条思路的理论依据正是学姐说的——BP 译码是马尔可夫过程,可用 RL 优化。我们把它**第一次**带到
空间耦合码的滑窗译码场景。

---

## 1. 文献调研与新颖性

把"机器学习 / 强化学习 + LDPC 译码"按三层梳理:

### ① RL 优化【分组】LDPC 译码 —— 已成熟
- Habib, Beemer, Kliewer,*Learning to Decode: RL for Decoding of Sparse Graph-Based Channel Codes*,
  NeurIPS 2020 / IEEE JSAIT 2021。把校验节点(CN)调度建成 MDP / 多臂赌博机,用 **Q-learning** 学序贯调度。
- **RELDEC**,Habib et al.,IEEE TCOM 2023(arXiv 2112.13934)。中等长度**分组** LDPC(含 5G NR),
  学习 CN **簇调度**;元强化学习(M-/AM-RELDEC)应对不同 SNR。
- Carpi et al.,*RL for Channel Coding: Learned Bit-Flipping Decoding*,2019;以及 D3QN(2025)、
  Action-List 综合征译码(2025)等。
- **共性:全部针对分组码 / 泛洪或序贯 CN 调度,与空间耦合、滑窗无关。**

### ② 学习方法用于【空间耦合】LDPC 滑窗译码 —— 仅一篇,且非 RL
- Yun et al.,**Neural Window Decoder (NWD) for SC-LDPC Codes**,arXiv 2411.19092,2024.11。
  这是迄今唯一一篇 SC-LDPC + 学习 + 滑窗的工作。但它是**梯度训练的深度展开网络**:学习固定的
  神经权重 + 阻尼因子,并用一组"抗错误传播权重"在检测到错误时切换。**完全不涉及强化学习 / MDP。**

### ③ SC-LDPC 滑窗调度的传统(非学习)优化
- 可靠性窗口译码、非均匀窗口调度、PMR(部分消息保留)抗错误传播等——均为**人工启发式**。

### → 空白点(本课题的新颖性)
| | 分组 LDPC | 空间耦合 LDPC 滑窗 |
|---|---|---|
| 启发式调度 | ✓ | ✓ |
| 梯度/神经网络 | ✓ (Neural BP) | ✓ (NWD, 2024) |
| **强化学习 / MDP** | ✓ (RELDEC, Habib) | **✗ ← 本工作** |

> **没有任何已发表 SCI 论文把 SC-LDPC 的滑窗/序贯译码建成 MDP 并用 RL 优化。** 这是本课题的立足点。
> 而 SC-LDPC 比分组码更适合 RL:译码波天然是从两端向中间的**序贯**过程,滑窗"每步推进一格"
> 天然是 MDP 的时间步,错误传播是智能体可主动学会规避的现象——这些都是分组码 RL 工作里没有的结构红利。

---

## 2. 把滑窗译码建成 MDP

记 SC-LDPC 耦合长度 `L`、耦合记忆 `w`、窗口宽度 `W`。滑窗译码按 `t = 0,1,…,L-1`(译码波方向)推进,
每个位置 `t` 在一个 `W` 位置的子图(window)上跑 BP,只**输出/提交目标位置 t 的块**,然后窗口右移一格。

- **回合(episode)**:译码一帧接收信号,从 `t=0` 走到 `t=L-1`。
- **时间步**:在当前窗口上做的**一次 BP 迭代**。
- **状态 `s`(6 维,全部可在线获得、不含真值,故策略可直接部署)**:

  | 特征 | 含义 |
  |---|---|
  | `f_leftsyn` | **最左(冻结)校验块**未满足比例——窗口推进后该块永久冻结,衡量目标与已定历史的一致性 |
  | `f_targmin` | **目标块最弱比特的置信度**(归一化 `min|LLR|`)← **关键预测信号**(一个高置信错误比特即致错) |
  | `f_streak`  | 目标块硬判决连续未变的迭代数(稳定性) |
  | `f_iter`    | 本位置已用迭代数 / 上限 |
  | `f_ep`      | 错误传播风险标志(上个位置是否在冻结校验未满足时就提交) |
  | `f_pos`     | `t/L`(边界 vs 体内) |

- **动作 `a`**:`COMMIT`(提交目标块、窗口推进)或 `CONTINUE@α`(再做一次 BP 迭代,
  归一化最小和缩放因子 `α`)。实测 `α=0.8` 近最优(见 §2.1 诊断),其余取值更差,故正式实验用
  **2 动作**(`α` 固定 0.8,只学**何时提交**);框架仍支持多 `α` 动作(作为消融,增益甚微)。
  另设**最小迭代下限** `min_iter` 禁止过早提交。
- **奖励**:
  ```
  CONTINUE :  −iter_cost  +  shape_cost · (目标可靠性的提升)   # 复杂度代价 + 稠密塑形
  COMMIT   :  −err_cost · (目标块比特错误数 / 块长)             # 仅训练时用真值,状态中从不含真值
  ```
  最大化回报 `−Σ迭代数 − err_cost·Σ错误`,即在**译码复杂度**与**误码率**之间权衡;
  扫描 `err_cost` 可画出一条帕累托前沿。

### 2.1 关键诊断(为什么 RL 能赢)
在工作点(BG2/Z=8/mp=8,码率≈0.60,L=30,W=6,Eb/N0=3.0 dB)实测每位置动态:

- **目标块平均仅 3.4 次迭代就译对**(标准差 1.7);
- 但整窗综合征**经常永远归不了零**(25 次迭代内约四成位置从未归零)——因为窗口含尚未充分约束的前瞻位置;
- **约 90% 的位置:在整窗综合征归零之前,目标块就已经正确**,之后平均**白白多迭代约 11 次**。

> 即:经典停止判据(整窗综合征)与译码器的真实目标(只输出目标块)**不匹配**,造成大量迭代浪费。
> RL 从奖励中自动发现正确判据——**目标块稳定可靠即提交**——这正是复杂度降低的来源,
> 也是塑形奖励要奖励"目标可靠性提升"而非"整窗综合征下降"的原因。

### 2.2 头部空间有多大?——预言机上界
为量化可达增益,用一个**预言机**(知道真值,目标块一对就提交,否则迭代到上限)测出自适应停止的绝对上界:

| @3.0 dB | 平均迭代/帧 | info BER |
|---|---|---|
| 固定窗口 `max_iter=20` | 383 | 1.9e-4 |
| **预言机停止** | **106** | **6.3e-5** |

预言机**同时**做到迭代少 3.6 倍、BER 还更低(早提交还避免了后续震荡漂移成错)。
**头部空间巨大,问题纯粹是"如何免真值地检测目标已正确"。** 实测发现决定性特征是
**目标块最弱比特 `min|LLR|`**:一个简单的固定阈值规则 `min|LLR_target| > T` 就已**帕累托压制**
标准译码器(如 `T=10`:246 迭代达到与 `max_iter=20` 相同的 1.9e-4,**省 36% 迭代**)。

### 2.3 可达性边界(诚实的负结果)
我们进一步穷举了**状态相关阈值**(安全时——冻结校验已满足且硬判决稳定——用低阈值早提交,
否则要求高置信)。结果:这些细化规则**都被简单固定阈值帕累托支配**(早提交省下的迭代被多提交的
错误抵消)。即:**在免真值信号里,`min|LLR|` 阈值已接近最优;预言机剩余的红利源于"知道真值",
不是任何可部署策略能拿到的。** 因此:

> **RL 的贡献不是"打败" `min|LLR|` 阈值,而是:(a) 仅凭奖励就自动发现并复现这一近最优策略
> (无需人工诊断出 `min|LLR|`),(b) 给出一个可扩展到更丰富动作(变步进、回滚、抗错误传播)的
> 统一 MDP 框架。** 同时,`min|LLR|` 早提交本身就是一个简单、可部署、省 ~30% 复杂度的新启发式。

---

## 3. 算法

纯 NumPy 的**表格型 Q-learning**(与 RELDEC 一致,可解释、零依赖,契合本仓库风格):

- 状态离散化(每维 3–5 个分箱)→ Q 表 `Q[离散状态, 动作]`。
- ε-贪婪行为策略,ε 线性退火;Q-learning 更新 `Q(s,a) ← Q + lr·[r + γ·maxₐ' Q(s',a') − Q(s,a)]`。
- 训练时在门限附近多个 SNR 上采样(状态含可靠性特征,单一策略即可自适应不同 SNR)。

**公平性**:基线(固定窗口译码器)与 RL 共用同一个 BP 引擎 `WindowBP`(已逐比特对齐原
`Tanner.decode` 与 `decode_windowed`),二者**只有控制策略不同**,任何增益都仅归因于学到的策略。

---

## 4. 实验设计

- **基线①**:固定窗口译码器(整窗综合征提前停止 + `max_iter` 上限),扫 `max_iter` 得一条
  (平均迭代数, BER)曲线——这是**标准 SC 滑窗译码器**。
- **基线②**:固定阈值早提交规则 `min|LLR_target| > T`,扫 `T`——这是本工作诊断得到的**强启发式基线**
  (已压制基线①);RL 应**自动学到与之同量级**的策略(无需人工指定 `T`)。
- **参考**:预言机停止(上界)。
- **RL**:训练若干 `err_cost` 的策略,得到若干 (平均迭代数, BER) 点。
- **图 1(`exp_rl_ber.svg`)**:BER vs Eb/N0,RL vs 标准译码器 vs 固定阈值,**复杂度匹配**——
  显示单一 RL 策略跨 SNR 稳超标准译码器、并追平固定阈值。
- **图 2(`exp_rl_pareto.svg`)**:固定 SNR 下 **BER vs 平均 BP 迭代数**的帕累托图——
  标准译码器一条曲线,阈值规则与 RL 点落在其**左下方**(同等 BER 更省迭代),预言机为左下角上界。
- 复杂度度量:平均每帧 BP 迭代数(以及等价的 CN 更新次数)。

### 结果

配置:BG2 / Z=8 / mp=8(分量码率 0.625)、`L=30, w=2`、窗口 `W=6`、`α=0.8`、`Eb/N0=3.0 dB`。
平均迭代数 = 每帧 BP 迭代总数(越小越省),info BER 越低越好。

**帕累托 @ 3.0 dB**(单次运行,400 帧/点):

| 方法 | 平均迭代/帧 | info BER | 相对标准译码器 |
|---|---:|---:|---|
| 标准滑窗 `max_iter=20` | 382 | 2.0e-4 | 基线 |
| 标准滑窗 `max_iter=15` | 316 | 2.7e-4 | |
| 标准滑窗 `max_iter=10` | 244 | 5.9e-4 | |
| **min-LLR 阈值 `T=9`** | **234** | **2.6e-4** | ≈ mi=15 的 BER,迭代 **−26%** |
| **RL `err_cost=2500`** | **259** | **2.6e-4** | ≈ mi=15 的 BER,迭代 **−18%** |
| **RL `err_cost=6000`** | **294** | **2.2e-4** | ≈ mi=20 的 BER,迭代 **−23%** |
| 预言机(上界) | 107 | 7.6e-5 | 迭代 −72%、BER 更低 |

**BER vs Eb/N0**(复杂度匹配,RL `ec=4000` vs 标准 `mi=12` vs 阈值 `T=11`):

| Eb/N0 | 标准 | 阈值 T=11 | **RL** |
|---|---|---|---|
| 2.5 dB | 2.9e-3 (305it) | 1.7e-3 (382it) | **1.6e-3 (343it)** ← 最优 |
| 3.0 dB | 3.8e-4 (274it) | 2.3e-4 (261it) | 2.5e-4 (260it) |
| 3.5 dB | 9.1e-5 (235it) | 2.7e-5 (196it) | 7.7e-5 (202it) |
| 4.0 dB | 1.1e-5 (199it) | 6.3e-6 (162it) | 2.2e-5 (162it) |

**结论:**
1. **min-LLR 早提交启发式稳健帕累托压制标准滑窗译码器**——同/更低 BER 下省约 **20–26%** BP 迭代,跨 SNR 成立。
2. **RL 仅凭奖励自动学到同量级策略**(无需人工诊断出 `min|LLR|`):在 **2.5–3.5 dB 稳超标准译码器**
   (3.0 dB 同 BER 省 ~18–23% 迭代)、**追平手工阈值规则**;4.0 dB 用更少迭代换略高 BER。
3. **预言机表明仍有约 2× 额外头部空间**,但源于"知道真值",不可由免真值信号达到(§2.3);
   要逼近它需连续/函数逼近策略(§6),是 RL 框架的自然延伸。

> 图见 `exp_rl_pareto.svg`(复杂度-BER 帕累托)、`exp_rl_ber.svg`(BER-SNR)。
> 复现:`python experiments_rl.py all`(纯 NumPy,约 16 分钟);低 BER 点(~1e-5)有统计涨落,趋势稳定。

---

## 5. 与最接近工作的区分

| | NWD (2024, 最接近) | RELDEC (2023) | **本工作** |
|---|---|---|---|
| 码 | SC-LDPC 滑窗 | 分组 LDPC | SC-LDPC 滑窗 |
| 学什么 | 固定神经权重+阻尼因子 | CN 簇调度策略 | **滑窗控制策略(停止/α/抗EP)** |
| 范式 | 梯度/监督(深度展开) | 强化学习 | **强化学习(MDP)** |
| 主要收益 | 性能↑或复杂度↓ | 复杂度↓ | **复杂度↓ @ 同 BER(目标判据)** |

---

## 6. 后续可扩展
- 动作中加入"窗口可变步进 / 回滚重译"以更主动地抗错误传播;
- 线性函数逼近 / 小型 MLP 策略(若引入 PyTorch)替代表格,处理更大状态;
- 元强化学习跨 SNR / 跨码率泛化(对标 AM-RELDEC);
- 与 NWD 的学到权重**正交结合**(RL 控调度 + 神经权重控消息)。
